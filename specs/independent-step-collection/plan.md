# 独立计步实验 App：自由活动执行计划

依据：已确认的[总纲](../../CONSTITUTION.md) 2.0.0、[需求](requirements.md)和[验收标准](validation.md)。沿用 `codex/free-living-session-spec`。本轮主题是 **登记并直接连接戒指**：在 T1b 上减少页面与重复确认，保持真实状态及持久化保护。

## 1. 当前状态与范围

| 已有工作 | 已完成范围 | 尚未完成 |
| --- | --- | --- |
| T0 基线，`2ac4688` | Android 源码脱敏入库；原版25项JVM和Debug构建通过 | Python工作副本及测试、原版未执行的实物项按独立版验收承接 |
| T1 准备与BLE修复，`77577de`、`4114292` | 独立身份、编号/位置/戒指记忆、只读设备查询；旧版准备页6次真机重连有证据 | 更广硬件/系统兼容及长期稳定性 |
| T1b 初版，`b8a2d29` | 四页准备导航；111项JVM、10项API31 Android测试和APK构建通过 | 旧页面方案由下行版本接替，历史证据见E10 |
| T1b 精简，0.6.3-t1（29） | 合并登记、直接连接与一次自动连接；114项JVM、17项API31运行测试、构建及真机只读路径通过 | API30定位、API33+返回手势、权限/系统设置全分支及长期稳定性待验证，详见E11/B20 |
| T2a，`1edcf67` | 身份冻结、请求/确认账本、原子写/目录同步；31项JVM及API31默认存储检查通过 | 尚未接UI/BLE，没有参考数字、下载补证和待传锁释放接口 |

总纲与研究流程保持不变。Q01编号更换、Q02确认后纠错按需求中的决策状态处理；Q03无法读数时记录原因并保全数据已确认。准备页只读设备状态，T2的安全出口尚未完成前不开启真实采集。

本轮复用原生 Activity、PreparationStore、RingPreparationController 和 BLE 队列。将编号和位置在同一次原子写入中登记；页面状态和主要动作采用统一判断。文件格式保持兼容，原编号/安装身份/位置/戒指信息继续有效。原始交接资料、本机配置及其他窗口修改保留。

## 2. 从当前版本到小规模试用的实施顺序

每个切片围绕一条可检查路径，同时验收 UI 和实际数据。表中待完成项不代表已经开放。

| 切片 / 状态 | 用户路径与交付 | 前置条件、复用和关键工作 | 通过条件 |
| --- | --- | --- | --- |
| T1b 精简准备 / 本轮 | 编号＋位置 → 保存并连接 → 按需授权及搜索 → 选择即保存 → 首页连接结果；已有戒指冷启动自动尝试一次，修改位置就地保存 | 复用现有准备存储和控制器；删除仅承担跳转的确认。自动连接只在前台且条件齐备时执行，取消后不循环；保存失败保留输入/原值，旧半份档案可补位置 | B03/B04a/B05/B20：原子保存、直接搜索/连接、一次自动连接、等待/超时/异常/就绪一致、重开保留；模拟器和真机分别记录 |
| T2b 请求与设备协调 / 下一步 | 开始请求 → 等待戒指证据 → 采集中；停止请求 → 等待停止证据；结果不明有查询恢复入口 | 在T2a上建立可测试协调器，先持久请求再发命令；关联连接代次、开始前STATUS/LIST及设备记录。前台服务持有唯一传输连接，页面订阅状态。先用替身检查，真实入口与T2c/T2d保全路径一起开放 | B04b/B06/B08/B18：写失败不发命令、重复点击不多建session、旧回复不推进、START结果不明不盲目重发、恢复保留身份 |
| T2c 结束与参考保全 / 待实现 | 停止证据保存 → 等读数稳定 → 输入一个总数或缺失原因 → 可靠保存 | 扩展账本，记录valid/missing/unreliable、填写时间和异常；0与null区分。Q03已确认，Q02在相关纠错功能实施前确认；保存后才能进入下载 | B07/B08/B15：数字、原因与session一致；写失败原页重试，重开不用重填；UI成功必须对应落盘 |
| T2d 原始数据落手机 / 待实现 | 参考已保存 → 下载进度 → 本地完整/可恢复失败 → 首页 | 复用HealthFlashDownload、rfbin v2与CRC/SHA；按已确认设备记录下载，补采集边界证据；保护临时原始文件，校验后才释放待传锁。核验B19旧记录边界 | B06/B08/B09/B13：中断续传不串文件、不重复；下载完成时间独立；本地完整后上传失败不阻挡新session |
| T2e 打包与上传 / 待实现 | 本地完整 → 上传中/待重试 → 上传成功回执 | 复用上传队列，审查原有存储回退；首次发送前冻结完整包。同session一个参考总数、多文件映射；失败或回执丢失重试原内容 | B09/B10/B13：UI、本地、manifest一致；手机实际上传并从目标云盘读回，重复包不产生第二条逻辑记录 |
| T3 后端契约与质量 / 待实现 | 同步同一测试文件夹 → 导入结果 → session参考表与50 Hz CSV | 从归档建立受控工作副本；活动同步退出Oura/Junction依赖。显式新旧版本校验、自由活动未标注、质量状态、一对多关联、幂等/冲突与跨午夜汇总；可在端侧开发时用合成包并行验证 | B04b/B07/B10–B14：pytest＋真实上传包；唯一参考数可追溯，未标注不变成已知类别，跨日总数不臆测拆分 |
| T4 端到端及失败恢复 / 待执行 | 被试独立完成真实采集 → 总数保存 → 上传 → 研究者正确读到；另做恢复与逐级长时 | T2/T3完整链路、指定手机/戒指/计步器、双端云盘配置；先短时/混合/零步/同日多次，再延长 | B02–B16实际结果矩阵；设备证据与软件测试分开，发布明确支持边界 |
| 小规模试用 / 待前述验收 | 在已验证版本上观察独立完成、错误、求助和数据质量 | 完整链路及关键失败恢复通过后审阅适用条件、培训与正式采集安排 | 冻结App/协议版本；人数、周期和质量阈值由设备试运行结果决定 |

T2b单独软件验收不等于允许被试开始正式采集。真实入口必须有停止、参考保全和原始记录保护的可用出口；开发测试使用明确标记的测试记录。

## 3. 本轮 T1b 行为与验收

本轮修改范围是准备 Activity、PreparationStore 的兼容登记接口、导航/存储测试及版本和规格。T2a账本、底层协议、BLE队列与旧传输组件继续复用，未在此轮接入采集。

1. **合并首次信息并继续连接。** 编号与六种位置同页，“保存并连接戒指”一次原子保存后检查权限及蓝牙条件，条件满足后直接搜索。失败不产生半份档案、不跳转，重建保留草稿；连接条件不足仍保留已确认信息。已有同编号登记保持幂等。
2. **首页直接呈现连接结果。** 冷启动恢复完整档案后，仅在前台、已授权且蓝牙/必要定位可用时自动连接一次。未满足条件显示恢复动作；失败后用户直接重试。选择设备成功保存后回首页连接，三项设备信息未齐时保持查询中；取消、返回、关闭弹窗、页面重建不会自动重复。设备详情提供更换戒指，选择后沿用同一可靠保存路径。
3. **按状态提供一个主要动作。** 等待时防重复；超时直接重试；权限/蓝牙条件不足进入对应设置；已有记录或正在采集说明问题并提供详情；全部检查成功自动呈现结果。
4. **就地修改位置。** 选择即写入，成功才更新已确认值；取消不变，同值不重复写。写失败保留原值并可重试，保存中防重复。
5. **保留必要指引。** 当前步骤、字段名、真实状态、异常处理保留；技术信息按需展开。清零、等待开始确认、站定停止及稳定读数提示在未来对应操作时呈现，数据含义保持总纲约定。
6. **复测后交付。** 单元测试、Android导航/文件测试、Debug构建、实际输入/返回/重开和连接检查；设备条件不足的项单列。原位安装保留既有档案。

页面流程设计见[同一FigJam中的精简准备修订](https://www.figma.com/board/q6WmkU3ViOqAfBN7tO301D)，实际行为以代码与B20证据为准。

## 4. 采集接入的复用边界与迁移顺序

旧 RingCaptureService 同时管理采集、身份和上传，不能把准备 Activity 直接绑定过去。源码核对发现：旧路径在发送START后建立身份、收到任意collecting状态自动接管、用下载完成时刻补结束时间。这些行为与新session契约冲突。

T2b实施前先说明并审阅以下技术迁移：由独立协调器管理持久请求和设备对应，前台服务独占既有RingBleClient；Activity只发用户意图和显示状态。先完成替身测试，再接服务/页面；原版服务与接口保留，独立入口逐段切换到经过验证的新路径。避免准备和采集两套控制器同时连接同一戒指。该方案调整职责，保持现有协议与文件格式；若进一步改变实验流程或数据语义，先确认需求。

具体边界：

- START/STOP返回成功只代表命令入队；状态展示依赖对应设备证据和账本成功写入。一次STATUS不能单独证明新记录归属。
- 对START结果不明的恢复先查询；设备重启、未知归属或边界丢失保持异常，不以手机当前时间补造真实边界。
- LIST按已确认的记录身份匹配，不能自动选“最新记录”归入当前被试。
- 停止确认、参考确认、下载完成分别持久；文件清单和后续边界证据可补齐，参考确认锁定不阻止处理字段更新。
- HealthFlashDownload转换失败后的临时文件删除、UploadSessionStore非原子回退为接入时的重点审查项。唯一未保全信号应继续留存。
- 旧实时IMU/PPG同步文件写入受主线程回调调整影响；若启用实时流，先将I/O移到有序后台并验关闭/落盘，准备查询通过不代表采集性能通过。
- 新旧包采用明确版本分路。暂定容器v2/rfbin v2，新增step_schema_version=1与daily_activity_v2；实现前同步端侧和Python契约，避免旧白名单拒收。

## 5. 实物前置条件与验收门禁

指定约5MB旧记录已获准无需保留，无需新增恢复/迁移任务。现有证据仅证明设备当时空闲并报告记录量；仓库没有固件实现或证明START覆盖方式的资料。新采集前核对同一设备、空闲状态及无新实验待保存记录，保留STATUS/LIST基线，再受控执行单次START、查询确认、STOP和LIST，结合原始片段核对新旧记录边界。此授权只覆盖指定旧记录；新数据继续受防丢失保护。

真实采集验证需要：手机解锁并允许系统权限、指定戒指有电、计步器型号与统一佩戴方式、可核对的云盘读取配置。手机上传配置与Python读取位置必须一致。真实链接、账号、凭据和绝对本机路径留在本地配置。

长时验证按短时、约1小时、数小时逐级推进，再依据证据探索接近一天；这些是测试梯度，不是软件限制。每一级核对电量、存储增长/满后行为、样本覆盖、手机下载与峰值空间、上传/解码耗时和恢复。软件超时、时间窗口和解压限额不能当作设备支持时长。跨午夜只有一个参考总数时保持整段，不拆分两日真值。

进入小规模试用前须同时满足：

- 一名按指导准备的测试者独立完成真实采集，混合活动只填写一个同时间段总数。
- 有效零步、缺失、不可靠记录可分辨，保存/重启后保持一致。
- 手机、manifest、云盘文件和后端记录逐字段对应，原始文件/派生CSV可追溯。
- 重启、断连、下载中断、上传失败/回执丢失的关键恢复路径通过，重复导入不重复计数。
- 已验证时长、设备/固件与未覆盖条件明确；再确定人数、周期和质量阈值。

## 6. 团队交付与验证方式

每轮完成“行为与验收 → 实现 → 自动检查及独立审查 → 构建安装 → 实际操作 → 修复复测 → 更新记录与本地提交”。涉及蓝牙、状态、持久化、恢复、上传/导入的修改，独立审查提供文件位置、触发条件、影响与依据，主代理核实后修复。

Android复用Gradle/JUnit与指定instrumented tests，Python复用pytest。UI状态与实际采集、保存和回执共同验收；截图不能替代文件及操作证据。模拟器负责日常页面/本地/合成异常，真机负责BLE、真实数据与长时。复现命令和版本结果集中在[validation.md](validation.md)。

共享文档保留当前行为、关键设计依据、可复现方法、脱敏结果和限制。原始日志、截图、本机路径和个人偏好留在忽略的本地目录；详细历史通过Git追溯。功能变化更新requirements/plan/validation及已有change-review，不新建平行总纲。每个独立小步审阅并验证后只提交相关文件，保留其他窗口改动；远程推送、发布和新的实验决定按既有授权范围执行。
