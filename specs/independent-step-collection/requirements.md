# 独立计步实验 App：自由活动 session 需求

状态：总纲2.1.0已确认；T1b、T2-P和全流程配色分别按E11、E15–E17收口。当前0.7.0-local已完成两段真实短采的参考保存与原始文件落盘，其中第二段通过正常开始、后台、断连恢复和停止流程；第一段含工程恢复，分别记录于E18。重开保留已验证，时间对齐、完整故障矩阵及长时能力继续验收。云盘上传与研究端读取待T2e/T3接入。Q01/Q02待确认，Q03已确认。更新日期：2026-09-19。分支：`codex/free-living-session-spec`。

本规格服从用户已确认的 [CONSTITUTION.md](../../CONSTITUTION.md) 2.1.0；项目使命、技术栈和总体路线由总纲唯一维护。实施切片见 [plan.md](plan.md)，验收见 [validation.md](validation.md)，旧设计与源码影响见 [change-review.md](change-review.md)。用户已授权按小切片持续实现；Q01/Q02 待确认，Q03 按 2026-09-19 已确认方案实施。

## 1. 本功能边界与当前事实

统一流程是“开始采集 → 正常生活 → 结束采集 → 填写本次计步器总步数 → 保存并上传”。例如 P001 从 09:00 采到 11:00，期间走路、坐着工作、短暂跑步，最后填写 562；全部对应信号和一个参考值 562 属于同一 session。当天 15:00 再次采集时先清零计步器，创建另一个 session。

本功能覆盖独立入口、自由活动采集、整段参考步数与异常、本地保存/恢复、云盘和本机 Python 闭环、研究侧 session 结果及日汇总语义。具体活动的时间标注、在线步数预测、分类准确性评价、正式人数与周期留在后续。首页日汇总卡片和图表暂不列为最近的 T1 切片交付；任何显示的日统计都遵守 S11。

当前证据与目标能力分开记录：

- Android 0.5.3 工作副本已在 `2ac4688` 脱敏并纳入版本管理，原始交接资料保留。在此基线上已完成独立入口、本地登记、设备准备和隔离演示。0.7.0-local复用相同页面与保存接口，接入真实BLE服务、请求证据、参考保存和rfbin下载；软件集成通过与真机采集通过分别记录，当前设备验收见E18。
- [历史 E01](../phase-0-baseline/validation.md) 有 25 项 JVM 测试通过记录。[最近验证](../../docs/upload-target-verification-20260918.md) 证明原版用 JDK 21 构建成功、测试云盘本地配置生效，以及电脑通过上传接口成功发送一个测试文本文件。
- [本轮桌面基线](../../docs/android-baseline-20260918.md) 已补齐源码对应，复现原版 25 项 JVM 测试通过和 Debug 构建通过；该结果适用于 T1 修改前的原版能力。独立版本、设备文件/UI、手机采集上传、后端读取和长时能力分别取证，阶段 0 的设备与 Python 项仍待验证。
- 用户已确认手机、戒指和充电盒可用，计步器可手动清零。2026-09-19 已用华为 P40 Pro+（用户报告 HarmonyOS 4.2，实测 API 31）读到戒指固件 1.2.66；计步器型号/佩戴说明及长时参数待补录或实测。
- 手机上传目标已配置为用户的测试文件夹；本机 Python 的读取凭据和保存路径尚待配置。实际链接继续只保存在忽略的本地配置中。

### 准备流程：保留T1b，接入真实采集入口

首次同页填写研究者分配的编号与佩戴位置，点击“保存并连接戒指”；保存成功后检查权限和系统条件，条件满足便直接搜索。选中戒指可靠保存后回首页连接，查询结果自动显示。登记可在离线或蓝牙不可用时完成，连接受阻时保留信息并提供恢复动作。0.7.0-local在准备完成后交接到真实采集服务；服务重新核对当前设备和待处理session，再决定能否开始或恢复任务。

- 本地保存规范编号、稳定的 installation UUID、已确认位置及所选戒指的地址/名称。编号一经登记，本切片保持固定；Q01 确认后再开放更换。重复登记相同规范编号不创建新身份。
- 准备信息写入成功后才显示保存成功。写入失败保留上一份确认信息并允许重试；已有文件损坏时保留原文件、提示研究者处理，阻止重新登记覆盖。
- 重开恢复准备信息；冷启动时，已有编号、位置和戒指且权限、蓝牙及必要定位条件齐备，自动尝试一次只读连接查询。条件不足时显示恢复动作，冷启动不自动弹出权限或设置界面；失败由用户重试。设备切换或断连后清除过期实时状态，不将上次连接记为当前连接。
- 准备控制器只执行扫描、连接、电量、INFO固件和HEALTH STATUS查询。进入真实采集前释放准备连接，由独立前台服务接管BLE；开始、停止、READ和session写入统一通过采集owner执行，旧采集服务保持独立。
- 未连接、权限不足、STATUS 未返回/超时、设备正在采集、状态错误或报告有记录时，准备结果保持未就绪或待核对。STATUS 的字节数/记录数只能表明戒指报告有数据，无法判断其是否已下载；不自动接管这些记录或赋予当前编号。
- 准备页保留必要字段、当前步骤、状态与恢复动作。自由活动、每次清零、站定及稳定读数提示在 T2 对应操作时呈现；旧注册、Oura、睡眠、Polar、评分和活动选择退出独立导航。

T1 的通过证据分别覆盖自动测试、APK 构建/身份检查和设备操作。真实戒指查询、应用强行停止后重开、升级与旧版共存未执行时保持待验证；准备流程通过不代表完整 session、上传或长时间采集已验收。

请求账本冻结session身份，分别保存开始/停止请求、确认、参考和本地文件；未知设备边界保持为空。0.7.0-local使用v3账本持久化完整开始基线和设备记录指纹，兼容读取v1/v2，在下次写入时升级；旧记录缺少的关联证据保持缺失。演示与正式任务使用独立目录。

### T1b：准备页面与导航

依据总纲 §5.1 和最新精简要求，本轮完成“登记并直接连接戒指”路径。页面与重复确认调整属于已授权范围，编号更换、参考纠错仍按Q01/Q02处理，实验流程和数据含义保持总纲约定。

| 页面 / 入口 | 主要操作与下一步 | 保存、返回与异常规则 |
| --- | --- | --- |
| 首次登记 / 第1步 | 同页填写编号与六种佩戴位置之一，点击“保存并连接戒指”；保存后按需授权并直接搜索 | 一次原子写入；失败无半份档案、不跳转，保留输入。连接条件不足时到首页处理，已保存信息保留。已有编号固定，旧版未填位置档案可补位置 |
| 首页 / 第2步 | 紧凑展示已保存信息和一处设备状态；已有戒指且前置齐备时冷启动自动连接一次，失败直接重试；未选戒指时可直接搜索 | 查询期间防重复，三项设备信息齐全后自动更新结果；有记录/采集中提供核对入口。连接完成直接呈现结果，无“完成连接检查”确认；设备详情提供更换戒指入口 |
| 选择戒指 / 按需进入 | 搜索列表中选择设备，保存成功后回首页自动连接 | 未选可返回，搜索可停止/重试；设备保存失败不连接，保留原准备信息。离页隔离过期扫描回调 |
| 修改位置 / 首页弹窗 | 选择即保存，成功才更新首页并关闭；无二次保存按钮 | 取消不变，同值不重复写；失败保持原值并可重试，保存中防重复。当前准备位置只影响将来session |

每页用当前步骤和一个主要动作表达下一步，正文只保留完成任务所需内容。主要操作固定底部，内容可滚动，键盘打开后仍可访问。首页状态与按钮使用同一判断：查询未齐不显示就绪，真实保存成功才反馈成功。固件、地址、记录计数与备用设置入口放入设备详情；蓝牙关闭、Android11定位关闭、权限拒绝等阻碍发生时直接提供对应恢复动作。

准备页面切换保持本次只读连接，退出准备功能时沿用T1断连规则。每次冷启动最多自动尝试一次，取消连接、页面返回、关闭弹窗和屏幕重建均不触发准备页循环重连。采集服务接管后，后台连接与任务恢复按T2规则运行，准备页不再同时持有BLE。首次登记重建保留编号和位置草稿，冷开读取已确认信息。位置弹窗选择前取消或重建不改变已保存值；选择后的写入以实际持久化结果为准。开始前清零、设备真实开始/停止证据、参考值可靠保存与异常处理继续保留。B20检验准备路径，服务交接与生命周期另按B08/B18取证。

### 完整流程与最小视觉约定（2026-09-19）

T1b 按 E11 收口：登记、选环保存、一次自动连接、只读查询及恢复保留现有实现；系统兼容、手机重启与长期稳定性仍按原验收项继续。后续以同一原生 App 逐段接通 T2，准备页不再单独反复改版。

完整设计参考复用[现有画板](https://www.figma.com/board/q6WmkU3ViOqAfBN7tO301D?node-id=18-103)。T2-P已在同一原生App中实现可操作页面，模拟标记覆盖整个演示流程；正式设备入口继续依据真实连接结果呈现。核心页面为首次登记、首页、采集中、参考读数、保存与传输；设备选择按需进入，开始/停止的等待与失败在当前任务中展示。

| 路径 | 当前与下一步 | 返回与恢复 |
| --- | --- | --- |
| 首次登记 → 选戒指 → 首页 | T1b已有；信息保存成功后搜索，选环保存成功后连接，完成自动更新 | 保存失败留原页；返回保留已确认准备信息 |
| 日常打开 → 首页 | 已实现：保留一次准备连接，真实服务优先恢复未完成session；完成记录可回首页查看 | 文件安全落手机前保持新开始保护；设备身份、指纹或时间依据不足时保留待核对。手机重启与后台存活仍需真机验收 |
| 首页 → 等待开始 → 采集中 | 已接真实控制：站定/佩戴/清零提示就地出现，先保存请求和基线，再等待完整匹配设备证据落盘 | 等待期间禁止重复开始；超时/断连仅查询当前证据，归属不明不重发START |
| 采集中 → 等待停止 → 参考读数 | 已接真实控制：停止请求先保存，匹配STATUS/LIST确认后等读数稳定，填整段唯一总数 | 返回或切后台由服务持有任务；停止无法确认时可先保存异常读数及原因；请求/确认与未知真实边界分开保存 |
| 参考读数 → 下载 | 正式入口使用“保存本次记录”，数字原子保存并读回后才发READ；有效0、缺失及不可靠读数分别保留 | 写失败留在输入页；缺失按Q03保存原因；确认后不开放修改，最终纠错方案仍待Q02 |
| 下载 → 本地完整 → 首页 | 已接真实下载：按确认记录续传，核对完整字节、记录数、rfbin与哈希，账本可靠保存后显示本地完成 | 临时文件在完整落盘前保留；失败恢复原任务。当前版本保持上传pending，不发送实验云盘请求；自动上传与研究端读取由T2e/T3接续 |

最小视觉规范采用Quiet UI，0.6.7-t2p按已确认的五色色板统一现有准备和完整流程页面：

| 颜色 | 用途与搭配 |
| --- | --- |
| 灰色 `#EAE7E7` | 页面背景，衬托信息容器 |
| 米白 `#F9F8EB` | 内容卡片、输入与次要操作区域；深青主按钮上的文字 |
| 鼠尾草绿 `#76B39D` | 采集、连接和结果状态区域；区域内文字统一用深蓝 |
| 深青 `#155E63` | 主要按钮、可操作链接、焦点及进度；主按钮搭配米白文字 |
| 深蓝 `#132238` | 标题、正文和失败说明；演示标记使用深蓝底、米白字 |

派生中性色为辅助文字`#535E6A`与描边`#868D92`。普通文字与实际背景的对比度至少4.5:1，大字至少3:1；鼠尾草绿区域内不使用辅助灰文字。颜色之外，加载明确当前动作，成功依据实际保存、校验及对应环境的回执，失败用明确文字和重试入口表达。禁用操作使用独立的背景与文字状态，保留可读性，不对控件整体降透明度。

沿用原生系统字体：标题28sp、正文16sp、辅助14sp、演示标记13sp、普通输入18sp、步数输入32sp；计时保持单行，在24–44sp之间适配可用宽度。边距20dp、间距8/16/24dp；信息以24dp圆角卡片分组，主要按钮至少56dp高，其他交互目标至少48dp，按钮与下拉选项随文字增高。每页一个主要动作，返回/详情/异常为次要入口；正文保留当前操作所需的一句提示，字段名、状态码及内部检查规则留在诊断和工程记录中。登记、连接、首页、采集中、参考填写、下载/上传、成功和恢复页，以及弹窗、下拉选择、菜单、输入焦点和系统栏共用主题。根布局统一处理系统栏与键盘占位，内容可滚动，键盘收起后恢复内容位置；字号放大、窄屏及无障碍阅读随相关页面验收。配色已按E17收口，后续页面沿用该规范，优先修复影响任务理解或操作的问题。

页面只提交意图和显示状态。0.7.0-local由单一前台采集服务持有连接、协调器与任务；准备页先完成连接交接，页面返回或进入后台保持待完成任务的owner。空闲离页可释放连接；已释放owner忽略旧页面意图与迟到回调。后台存活及手机重启按真机条件验收。

### 真实本地闭环的软件边界（T2b.2/T2c/T2d）

0.7.0-local已接入`RealCollectionService`、串行采集owner和原始下载适配器。START/STOP请求先持久化，命令入队、设备观察与账本确认分别处理。开始前取得空闲STATUS与完整LIST；非空基线必须匹配已核对的旧记录许可，或已完整保全且哈希验证通过的设备记录。许可只对指定记录生效，B19的新旧边界与覆盖机制继续实测。

开始确认要求同一连续连接中的唯一可区分新记录；v3账本保存基线、session ID、uptime/Unix指纹和计数。固件可复用数值session ID，已核对旧基线中的同ID记录只有在两个非零锚均改变时，才可认定为新记录；单锚变化、同指纹和额外未知记录保持待核对。恢复须完整STATUS/LIST与已确认指纹、单调计数一致。只有开始请求、没有已确认指纹时，重开保持待核对。`unixMs=0`且有有效uptime的新ID记录可在同一连续连接完成开始、停止和下载；跨连接或进程后缺少boot ID佐证，保持待核对，不重新认领或读取。已有停止请求仅查询确认，不自动重发STOP；观察到无请求的停止也不补造用户操作。

读数可靠保存后才启动READ。下载按冻结的session、戒指与记录指纹组织，断点保存连续偏移和前缀SHA；完成时核对总字节、packet记录数、无缝或一致重叠、完整解析、rfbin头及载荷重读CRC，最终文件以SHA-256入账。完整文件和账本保存后，临时文件清理失败不会回退本地完成结果。协议没有DATA级session ID或设备CRC，连接代次隔离和完整记录查询由owner负责；手机CRC不能单独证明空中数据与设备Flash逐字节一致。

原始uptime保持原值，首末packet和LIST锚均不直接补造实际采集边界；当前样本覆盖记录为`not_assessed`。这些规则已有软件回归，实际记录身份、停止定稿、信号覆盖和后台行为继续按E18/B06–B09/B19验收。正式版本当前以“数据已保存在手机”为终点，T2e/T3接通后再显示真实上传回执。

### T2-P：完整路径的开发演示（已实现，证据E15）

Debug准备页通过右上角“更多 → 流程演示（模拟）”显式进入，入口在未登记和蓝牙条件不足时也可达。准备页主要操作只负责实际登记/连接；演示Activity不导出，Release保持原设备详情入口。演示使用既定顺序，覆盖独立体验登记、日常打开、开始/停止等待、参考填写与保存、模拟传输及失败恢复。研究流程与数据含义仍以总纲为准。

0.6.6的恢复首页按实际任务展示：采集中可“查看采集”，停止确认后可“填写步数”，下载中可“查看下载”，失败则重试或重新连接。浏览页面与后台任务进度分别维护；查看在途任务只导航，首页异步更新保留当前浏览位置。新开始条件继续依据请求、保存和文件状态；本地完整后旧上传独立进行。最近记录只列本地完整段，待填数字与明确缺失不会混用。采集中去除活动切换说明，填数页去除零值特例说明，0值校验保持原义；开始时间采用纯数字`yyyy-MM-dd HH:mm`，按session保存的时区格式化，保留原确认时间及真实采集边界字段。

- `StepCollectionActivity`通过结构化状态呈现原生页面；演示复用`FreeLivingCaptureCoordinator`和`FreeLivingSessionStore`，设备及传输结果集中在Debug适配层。应用进程中的单一串行owner持有任务，页面订阅/退出不取消任务。全程显示“设备与传输为模拟”；真实服务的生命周期能力仍由T2b.2验证。
- 示例编号、戒指、session、合成文件、参考数字和任务保存在应用私有`files/collection-demo/`。本地保存、读回、文件归属和SHA-256校验实际执行；正式目录保持原样，演示不发送真实BLE命令或云盘请求。发布构建不含演示入口，正式连接失败保持真实失败状态。
- 开始/停止按模拟设备证据确认；参考有效0、尚未确认、missing与unreliable分别表达。有数字记录确认时间，无数字时`ground_truth_recorded_at_ms`保持null，原因保存时间另记。原子保存失败留在输入页，可靠保存后才进入文件阶段；停止不明时可先保存不可靠读数或缺失原因，真实`started_at_ms`/`ended_at_ms`仍为null。
- 合成文件完整保存前保持当前任务保护；保存校验后，旧段待重试上传与下一段采集可独立进行。进程重开恢复当前任务及归档中的待处理传输，FAILED任务由用户重试；重复保存、下载和回执处理保持同一session。演示设备的持久UUID证据仅支持测试恢复，真实戒指的归属和边界仍须另取证。
- “演示选项”提供开始/停止确认超时、原子保存失败、下载失败、上传失败及断连。B23/E15记录23项运行层JVM、23项新增账本JVM及8项原生页面测试，并保留已有测试的适用范围；完整版本合计193项JVM、25项Android及Debug/测试APK/Release构建通过。
- Q01/Q02继续待确认；演示未开放更换正式编号或已确认读数修订。真实本地闭环按E18/B06–B09取证，云盘读回、后端和长时能力随后接通与验证；合成文件与回执只用于软件验证。

## 2. 可追踪需求

| 编号 | 本功能要求 | 实施切片 | 验收 |
| --- | --- | --- | --- |
| S01 | 独立显示名、applicationId 与存储目录，可与旧版共存，只处理本 App 的记录 | T1 | B02 |
| S02 | 研究者分配编号，本地校验并记住；session 冻结身份快照；离线登记及原位升级保留身份 | T1、T1b、T2、T3 | B03、B04a、B04b、B20 |
| S03 | 记住已确认佩戴位置，准备步骤、主要操作和返回路径清楚，提供统一开始入口；每次提示站定、佩戴和计步器清零，确认开始后正常活动；无需选择活动或固定时长 | T1、T1b、T2 | B05、B06、B20、B21、B22、B23 |
| S04 | 用户自主结束；请求、确认、真实采集边界及设备时间证据可追溯，结束后站定等读数稳定；下载不延长活动时长 | T2 | B06、B08、B16、B22 |
| S05 | 整段填写一个非负整数，允许零；确认后立即可靠保存来源、数字及填写时间。缺失、不可靠与有效零步分别表达，四端数值/状态一致 | T2、T3 | B07、B08、B10、B11、B15、B23 |
| S06 | 保留 BLE 重连、下载断点、本地持久化、上传重试；恢复保持 session、身份、已确认边界/数字及冻结内容，未知边界可据新证据补齐并留痕 | T2、T3 | B08、B09、B21、B22、B23 |
| S07 | 使用明确配置的实验云盘；未配或离线时保留本地任务，重试原内容；手机与 Python 指向同一位置 | T2、T3 | B09、B10 |
| S08 | 后端显式校验新旧契约并解码；独立活动入口退出 Oura/Junction/睡眠依赖；自由活动样本保持未标注 | T3 | B10、B11、B12 |
| S09 | session 包含的全部原始文件可追溯，按对应 IMU 信号汇总后只与一个参考总数比较；同日多次各自独立 | T2、T3 | B06、B13 |
| S10 | 简短异常入口记录清零/佩戴/计步器异常；保留不可靠或边界不明数据；完整性按设备实际信号及原始时间证据判断 | T2、T3 | B08、B09、B15、B16、B21 |
| S11 | 每日只汇总有效、互不重复的已采集时段；保留覆盖与缺失；跨午夜单一总数单列未分配，全天含义另有覆盖证据 | T3 | B14 |
| S12 | 灵活时长按短时到长时验证；测量电量、容量、手机空间、传输和解码资源；公布实测支持边界及异常处理 | T2、T3、T4 | B16 |

## 3. 操作与状态

### 首次准备和身份

研究者指导完整操作，依据计步器说明统一佩戴。App 显示并记住编号与戒指佩戴位置，后续开始前可确认位置是否仍正确；位置变更影响以后新 session，采集中元数据快照保持不变，途中换位通过异常记录表达。

应用显示名为“步数采集”，applicationId为`com.nexthci.ringfitness.steps`；真实session和rfbin保存在独立应用私有目录，演示目录另行隔离。既有公共导出目录`Download/RingFitnessSteps/`保持独立命名；当前开发版本为`0.7.0-local`。编号采用3–24位ASCII英文字母或数字，去首尾空格并统一小写；session的`participant_id`、`participant_name`均使用该规范编号。installation ID标记安装，session UUID在持久化开始请求时创建，再等待戒指确认采集。研究者维护发放表，离线登记只做格式校验。编号更换见Q01，当前保持固定编号。

升级、重开或手机重启保留已有身份与任务；卸载或清除应用数据后的自动任务恢复不在本次范围。原版文件与历史交接资料保留。

### 每次采集

1. 被试站定、佩戴两种设备、清零计步器。App 展示简短提示；额外确认勾选是否必要留给可用性检查，清零由被试操作，App 无法自动读取计步器是否清零。
2. 点击开始，持久化唯一 session 及编号/位置快照，再发出命令。得到戒指开始证据后显示“采集中”；开始失败保留失败状态和日志，重复点击保持同一请求身份。
3. 被试正常生活；活动变化无需操作，App 展示采集和连接的真实状态。一天可多次采集，无固定钟点、固定时长或整日限定。
4. 被试站定点击结束。App 先确认戒指已停止，可靠保存结束边界及证据；未确认时显示停止处理中，重连后查询实际状态。
5. 被试等读数稳定，输入整段总数。当前正式按钮为“保存本次记录”；确认数字后立即持久化，再下载对应Flash原始记录。T2e接通打包与真实上传后更新相应操作与结果反馈。正常路径只输入一个总数，途中步数合并计算。
6. 简短异常入口暂定为“忘记清零／临时摘下戒指或计步器／计步器意外归零／其他异常”。选项可附简短说明和已知时间范围；时间不清楚时明确保存未知。无法提供数字的保存交互见 Q03。

主状态为“准备 → 启动中 → 采集中 → 停止确认中 → 待填参考 → 参考已保存 → 下载/本地校验 → 排队上传 → 上传完成”。缺失参考可在明确记录原因后进入异常保存分支；传输进度与数据质量分别展示，例如“已上传，参考值待核对”。

异常保全路径：戒指断电或状态丢失而无法确认停止边界时，先保存当时可取得的计步器读数、观察时间与异常；有数字标为 unreliable，无数字保留 missing。界面继续显示停止/边界待核对，未知 ended_at_ms 为空。设备恢复后查询状态，确认可安全读取时继续下载；未落手机的原始记录保持待传保护。若边界最终无法恢复，由研究者决定分析资格。该路径保存的是不可靠参考证据，不能据此宣称正常采集已完成。

前一段戒指原始数据未安全落到手机前，保持待传保护，避免新任务覆盖 Flash。文件完整保存并校验后，云端重试应与下一段采集并行独立；用户无需等网络恢复才清零开始下一段。戒指能否同时容纳多个尚未下载的 session 暂不作为产品能力。

## 4. 数据契约与分析语义

下表为暂定字段方案，行为含义遵循总纲。原始 `.rfbin` 保持 v2；容器 `manifest.version = 2`，增加 `step_schema_version = 1` 区分新参考步数契约。新活动 schema 暂定 `daily_activity_v2`。三个版本分别表达容器、步数契约和活动元数据，由双端显式校验；不能仅加枚举而继续旧标签逻辑。

本地账本已落实参考值、来源、状态、参考保存时间、文件归属/校验及传输状态。v3增加`start_baseline`、`device_record_evidence`和`device_association_invalidated`；这些字段用于记录归属与恢复，不替代真实时间边界。真实文件本地完成后保持上传pending；新ZIP清单生成与后端契约由T2e/T3实现。未填写时`reference`为空；确认缺失时保存原因与`reference_saved_at_ms`，数字及`ground_truth_recorded_at_ms`保持null。演示文件与回执显式标记`simulated`，保持独立。

| 字段或字段组 | 语义与校验 |
| --- | --- |
| `session_id`、被试编号、佩戴位置 | UUID 唯一；身份/位置为创建时快照；同日不同 session 分开保存 |
| `capture_purpose`、`activity_schema`、`activity_code` | 分别为 `daily_activity`、`daily_activity_v2`、`free_living`；表示采集情境 |
| `activity_label_status`、`activity_label_source` | 自由活动固定为 `unlabelled`、`none`；逐样本真值标签为空。与旧 `other` 已知类别分开 |
| `started_at_ms`、`ended_at_ms` | 经证据支持的采集边界，UTC Unix 毫秒；无法确定的端点为空并标记 `capture_boundary_status = uncertain`。已确认端点保持不变 |
| 请求/确认、设备时间和覆盖信息 | 分别保存开始/停止请求与确认时刻、戒指设备 session ID、uptime/Unix anchor、首末样本及时间来源/修正记录。`capture_boundary_status` 暂定 `confirmed / uncertain`，另记时区与偏移。手机回调时间只证明收到确认 |
| `ground_truth_source` | `external_pedometer`，表示本实验指定参考来源 |
| `ground_truth_steps` | 确认的整数范围为 0 至有符号 64 位整数上限；缺失为 null。UI/后端拒绝负数、小数、布尔值、单位文本和溢出，不以工程上限宣称生理合理性 |
| `ground_truth_status` | `valid / missing / unreliable`。valid 要求确认整数，missing 要求 null；unreliable 可保留实际填写的非负整数或 null，并附异常原因。是否可用于分析另由完整性和边界共同决定 |
| `ground_truth_recorded_at_ms` | 确认数字的 UTC Unix 毫秒；有数字必有确认时间，无数字为空。异常说明保存时间另记 |
| `download_completed_at_ms` | 全部文件下载并校验后的处理时间，待完成时为空；与活动结束及填写时刻独立 |
| 文件清单 | 同一 session 下多个文件的名称、设备记录/片段 ID、字节数、SHA-256、已知时间范围/通道及对应派生文件，参考总数只置于 session 顶层 |
| `data_integrity_status`、`anomalies[]` | 完整性暂定 `pending / complete / incomplete / uncertain`；异常含类型、报告时间、已知发生范围或未知标记。完整性结论保留原始证据及处理版本 |

例：静坐记录确认 0 步，保存 `steps=0, status=valid`；尚未填写为 `steps=null, status=missing`；忘记清零后填写 562，保存 `steps=562, status=unreliable` 并记原因。三者均保留，后两者不自动进入正常参考评估。

时间校验需允许边界不明的异常档案被保存和导入。已知起止须有先后关系；单位、时区、手机改时/设备 uptime 重置等异常明确记录。发生不可解释的时间次序异常时保留为待核对数据，不靠改写原时间获得表面一致。时间对齐容忍度依据设备试运行决定。

### 同段信号与一个总数

计步器清零后保持站定，戒指确认开始后活动；结束时站定，确认停止后等读数稳定再填数，减少两设备边界间额外活动。实际样本覆盖和计步器延迟计数仍需设备实测。异常时保留数据，不从读数猜测缺失时间的步数。

一段包含多个 IMU 文件时检查顺序、范围及重复，再合并对应信号的估计步数，与唯一参考总数比较。PPG 通道、重传文件及重复片段不增加参考数或 IMU 样本。历史 `.rfbin` 中的实际原始字节保留。

后端为新契约开显式分支：旧 `daily_activity_v1` 包保留原有活动含义，缺少参考字段保持未提供；未知 schema/冲突组合进入拒绝或隔离记录。合法缺失/不可靠参考档案可导入，另标分析资格；格式损坏或类型非法不作为合法实验数据。

自由活动每行可携带 session 场景元数据，真值活动标签保持空值；下游训练/评价显式排除未标注标签，避免将其转为 `other=0`。后续算法预测另记 `label_source=algorithm`、模型版本与推理范围，与人工/实验标签分列。

### 每日汇总

研究侧先依据边界、参考质量、样本覆盖和重复检查，生成 session 是否可分析及原因，保留规则版本。暂定仅明确通过检查的记录进入普通总步数评估；未决异常交由研究者复核，纳入阈值在试运行后确定。

每日按采集时记录的研究时区汇总：同日有效且区间互不重叠的记录去重后相加，同时报告覆盖区间和缺失/待核对记录数。发现不同 ID 的区间或原始片段重叠时隔离候选并报告冲突，待明确去重结果后累计。未开始、未佩戴和漏采的时间保留缺失。

例：上午 562 步、下午 438 步，结果为“当天已采集时段累计 1000 步”，同时展示两个覆盖时段。23:50–00:20 的一段共 300 步，只保留 session 总数，在涉及日期单列“跨日未分配”；不能凭时长分成 100 和 200，也不能两天各加 300。跨日记录仍可用于完整 session 评估。

“覆盖完整目标时段的全天步数”需先定义该日目标时段，并有完整覆盖证据；当前尚无正式目标时段和覆盖阈值，暂不出具此结论。参考计步器总数与未来算法预测日总数分别命名、分别保留来源。

## 5. 持久化、恢复与长时边界

| 情形 | 处理规则 |
| --- | --- |
| 采集中断连、进程退出或手机重启 | 恢复原 session，查询戒指状态；BLE 缺连接和 Flash 缺信号分别记。找不到原始边界时标记 uncertain，当前时刻仅记录恢复事件 |
| 停止待确认 | 保留停止请求及已知证据，重连查询；断电/状态丢失时按异常保全路径先保存可取得读数，未知边界保持空值，待设备可安全读取后下载；无法恢复边界交研究者核对，原 Flash 保持保护 |
| 待填参考退出 | 恢复任务与已持久化草稿；草稿与已确认数值区分，保留 Flash 待传保护 |
| 确认参考时写入失败 | 明确保存失败，允许重试；不推进下载、压缩或上传。停止信息与原文件保持可恢复 |
| 数字保存后重启 | 从持久记录恢复数字、来源、填写时间和异常，继续原下载任务，无需重新填写 |
| 下载中断 | 原设备记录、session 与断点继续，完成后检查 CRC/SHA、片段重叠和缺口；下载重启不得改写采集边界 |
| 上传失败/回执丢失 | 使用同一冻结 ZIP、session 与哈希，保留原文件；排队自动恢复与失败后手动重试分别可检查。强行停止期间等待手动打开后恢复 |
| 数字/异常修订 | 最终确认前可修改；本地草稿变化使旧 ZIP 失效。参考快照的锁定按 Q02；下载时间、文件清单等处理字段随后补齐，首次发送前冻结整个 ZIP。已发送后保留原记录；后续纠错是否提供研究者补充记录见 Q02 |
| 未配上传目标 | 保留本地；首次配置后，仅为未绑定且从未发送的记录绑定明确目标。已绑定/尝试上传的记录保持目标，更换位置需要另行迁移规则 |
| 文件相同或冲突 | 同 ID 同包幂等；同 ID 异包显式冲突；同批副本和不同 ID 重叠信号另做去重/异常检查 |
| 低电、Flash 满、手机空间不足 | 能查询的量提前提示；失败时保留已知采集、参考与原始文件，标记截断/不确定并支持恢复；查询能力和提示阈值依据设备实测确定 |

App 不预设固定采集时长。长时能力按短时、数小时、接近一天逐级探索，每一级记录戒指/手机电量、Flash 实际容量或可验证上限、存储增长、下载耗时/吞吐、手机峰值空间及解码资源。先确认不会覆盖唯一未保存的实验数据，再做容量边界试验。发布可用上限必须带硬件、固件、采样配置和测试条件；接近一天当前只是目标场景。

现有解码/下载存在时间归一化逻辑，可能把超过 5 分钟的间隔重建为连续时间。完整性检查必须追溯原始包、uptime 与锚点，并记录修正；CSV 连续本身不足以证明无缺失。

## 6. 已确认、暂定与待确认

**已确认：** 自由活动、自主起止、一日可多段；每次清零；同段一个总数且允许 0；结束确认后填写并立即可靠保存；编号/位置记忆；独立计步器唯一设备参考；退出 Oura/睡眠；自己的云盘及本机后端；异常保留、恢复与长时实测。原 Q02“清零还是差值”已由用户本轮需求解决。

**当前工程方案：** 独立准备流程和隔离演示继续复用；真实服务接管、v3记录证据、参考保存与rfbin下载已集成，设备适配及恢复验收见E18。ZIP/后端schema、完整异常时间范围及研究侧日汇总仍待后续切片；编号更换与确认后的参考纠错保留Q01/Q02状态。

| 编号 | 待审阅的重要交互 | 建议、备选与影响 |
| --- | --- | --- |
| Q01 | 同一手机是否开放更换编号 | 建议设置中更换、采集/下载时锁定、只影响新记录；备选为研究者管理固定编号。前者便于纠正首次误填，旧任务归属保持不变 |
| Q02 | 确认保存后的纠错方式（承接旧 Q03） | 建议确认前可改，确认后锁定参考快照并自动推进；下载/文件等处理字段继续补齐，首次发送前冻结完整包。后来发现填错时保留原值，记录待核对，研究者另存修订。备选为首次发送前允许编辑，需额外取消队列、重建 ZIP 和竞态处理。上传后直接覆盖会破坏已有幂等规则 |
| Q03（已确认） | 无法读出总数时如何结束 | 用户于 2026-09-19 选择“允许：记录原因并保全数据”。异常入口提供“无法提供读数”，填写原因后以 null/missing 保存，再保全戒指信号；不作为零步。原始数据安全落手机之前仍保留待传保护，后续按已确认的数据链路上传 |

设备型号/固件、电量与容量、时间对齐/丢样容忍度、正式人数与周期属于待补证据或后续实验决策。本轮不把“两天试运行、七天采集”固化为安排或软件限制。
