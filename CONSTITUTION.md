# RingFitness 步数采集系统项目总纲

**版本：** 3.3.1

**修订日期：** 2026-09-21

**状态：** 已由用户确认；功能规格中的待确认交互单独保留

**项目阶段：** 用户实验与数据采集

## 1. 文档地位

本文件是项目的唯一总纲，也是任务范围、技术选择、开发顺序和验收标准的最高依据。

原始交接包、会议录音和历史代码用于提供背景与实现证据。每个开发阶段可以建立功能规格，但功能规格必须服从本总纲。项目目标或基本原则发生变化时，应先修订本文件，再调整功能规格与代码。

## 2. Mission：项目使命

### 2.1 研究目标

为自研 Ringo 智能戒指收集真实日常生活中的运动信号，以及同一采集时段的独立计步器参考总步数，用于后续步数估计与评估。基本研究单位是用户自主开始至结束的一次 session；第一阶段评估整段信号的总步数估计。独立计步器是本次计步实验的唯一设备参考来源，支持每次采集前手动清零。

### 2.2 目标用户

- **被试：** 使用 Android 手机、Ringo 戒指和独立计步器完成日常采集。
- **研究者：** 配置实验、检查上传状态、整理数据并开展算法分析。
- **初学开发者：** 通过真实项目逐步学习 Android、Web 后端和算法开发。

### 2.3 核心体验

研究者可分配被试编号（例如 P001）。登记时明确选择“研究编号”或“本地名称”：研究编号规范化后作为研究身份；本地名称只保存在手机，App 另行生成稳定匿名编号。输入类型不得仅依据字符形式推断。App 记住当前身份、显示名称和已确认的戒指佩戴位置；计步器佩戴方式依据设备说明统一。

被试每次执行以下流程：

1. 在首页选择本次“走路”或“跑步”；每段重新选择，无默认值。
2. 站定、佩戴好两种设备，将计步器清零，点击“开始采集”；等待 App 确认成功后进行所选活动。
3. 本段活动选择在开始时保存，采集中保持不变。需要换活动时，先结束本段并完成读数和原始数据的本地保存。
4. 站定，点击“结束采集”，等待 App 确认戒指停止并保存采集结束信息。
5. 戒指确认停止后进入收尾页，选择“保存并上传”“保存，稍后上传”或“放弃本段”。保存时等计步器读数稳定，填写本次总步数；App 确认数字后立即可靠保存。
6. 两种保存方式均继续下载并校验原始文件；手机保存完整后可回首页重新选择活动、清零并开始下一段。上传独立后台执行；选择稍后上传时持续暂停，直到用户在记录列表主动上传。
7. 放弃须二次确认，无需填写步数；仅取消并清理本段手机任务和文件，排除实验上传与统计，保留必要的放弃标记。戒指端操作以已核实的协议能力为限，不以整机清空或新START代替删除。

走路与跑步分别建立 session，每段开始前清零一次，结束后填写该段唯一总数。例如先完成一段走路并保存，再清零计步器，另开一段跑步。用户自主决定每段时长，一天可有多次 session；实际最长支持时长通过电量、存储和传输实测确定。间歇休息或误混其他活动时保留本段原始数据及异常说明，供研究者核对；一个总数无法拆成各活动的参考步数。

界面应始终让被试看清当前状态、下一步操作和数据是否已经安全保存。被试端主导航收敛为唯一首页、同一采集页、同一收尾页和记录入口；连接、开始确认、停止确认、下载、上传及可恢复异常在对应页面原位更新。加载状态使用进度提示，只有可以执行的动作才呈现为按钮。

### 2.4 当前范围

本项目目标版本包含以下能力；实际完成情况以功能规格的验证记录为准：

- 具有独立名称和安装身份的 Android 原生 App，可与原 RingFitness 并排安装；
- 本地身份登记与记忆，支持研究编号或仅在手机显示的名称；研究数据始终使用稳定匿名编号；
- Ringo 戒指 BLE 扫描、连接、采集控制和 Flash 数据下载；
- 每段明确选择走路或跑步，选择随 session 保存；
- 保留原版六种戒指佩戴位置：左／右手的食指、中指、无名指；独立版记住最近确认的位置，每个 session 保存创建时快照；
- 每次 session 的外部计步器总步数；
- 本地持久化、任务恢复、上传重试和上传记录；
- 清华云盘上传；
- Python 后端校验、导入和 `.rfbin` 解码；
- 面向算法的 50 Hz 加速度 CSV。

被试使用流程保留本地编号、戒指连接、佩戴位置、走路/跑步选择、采集控制、步数填写、简短异常记录和记录状态。旧平台注册登录、睡眠、Oura 授权与睡眠参考、Polar H10、旧六类活动与主观评分入口退出独立版本。实时步数预测、带时间标签的活动分类评价与卡路里估计属于后续研究阶段。

界面精简不得破坏共享的蓝牙重连、前台采集、状态恢复、Flash 断点下载、文件校验和上传重试能力。

### 2.5 成功标准

系统达到以下结果时，当前阶段可以验收：

- 被试能够独立完成走路与跑步两段采集，每段重新选择活动、清零、确认开始、结束填数并保存；
- 每个 session 保存编号、佩戴位置、本段活动选择及来源、采集边界及其证据、参考步数与质量状态；
- 有效零步、尚未填写和不可靠参考值可区分；异常记录保留原始数据供研究者判断；
- 已确认步数在 Flash 下载前完成本地持久化；
- App 重启、蓝牙短暂断开或上传失败后，session 与步数仍可恢复；
- 上传 ZIP 中的 `.rfbin`、`manifest.json` 和文件校验信息一致；
- 后端能够校验上传包并生成 50 Hz 加速度 CSV；
- 相同 `session_id` 的重试保持幂等；
- 完成走路后跑步两段独立保存，以及短时、逐级延长、误混/间歇异常、同日多次、零步及恢复场景的端到端验证；每项结论附实际设备与文件证据；
- App、本地记录、上传清单和后端记录中的参考值及状态逐次一致；
- 长时验证记录续航、存储增长、样本覆盖和下载耗时，公布经过验证的适用条件与时长；
- 日汇总只累计有效且互不重复的覆盖时段；缺失区间、跨午夜未分配记录与完整目标时段的覆盖状态可辨认。

开发交付以被试能够在已验证的设备与时长范围内正常使用、研究者能够正确读回数据为终点。真实设备、恢复、长时和独立操作检查属于开发验收；通过后交付可安装版本、操作说明及已知限制，直接支持实际采集。

## 3. Tech Stack：技术栈

### 3.1 Android 采集端

- **语言与构建：** Kotlin、Java 11 源码/字节码目标、Gradle Kotlin DSL；Gradle 运行使用已验证兼容的 JDK，具体版本见验证记录。
- **平台范围：** Android 11 及以上，`minSdk 30`，当前目标 SDK 36。
- **应用标识：** 独立版本采用与原版 `com.nexthci.ringfitness` 不同的 applicationId；具体名称和标识在功能规格中确定。
- **界面：** 继续使用现有原生 Android UI，在已有 0.5.3 项目上渐进式精简。
- **后台能力：** 使用前台服务维持 BLE 采集，使用现有上传任务机制处理长时上传。

选择原生 Android 的原因是现有项目已经实现戒指私有 BLE 协议、后台运行和故障恢复。复用这些实现，并在独立版本中分别验证实际设备行为。

### 3.2 戒指通信

- 使用现有 `RingBleClient` 与 `RingProtocol` 连接 Ringo 戒指。
- 使用 HEALTH 接口开始、停止、查询并下载 Flash 记录。
- 保留串行写入、通知处理、断线重连、稳定等待与下载断点。
- 固件协议没有变化时，沿用现有协议实现。

### 3.3 本地数据与上传

- 每次采集以唯一 `session_id` 作为数据单元。
- 戒指原始数据保存为无损 `.rfbin` v2。
- session 元数据先写入本地 JSON，再进入下载和上传阶段。
- `.rfbin` 与 `manifest.json` 打包为 ZIP，并通过 HTTPS 上传到清华云盘 Seafile 上传链接。
- 上传地址保存在本机 `local.properties`，代码仓库只保留配置模板。
- 手机只使用研究者配置的实验上传位置；Python 的云盘资料库、读取目录与之对应。采集与本地保存可离线完成，上传依赖网络和有效云盘配置。
- 匿名被试编号与本地显示名称在手机登记；显示名称不进入 manifest、CSV、云盘路径或研究端索引。原用户服务、Worker URL 与 enrollment code 从独立版本的运行依赖中移除。

### 3.4 Python 后端与 Web 工具

- Python 后端负责上传包校验、幂等导入、归档和 `.rfbin` 流式解码。
- 本地 Web 页面用于研究者的数据导入与预处理，不承担被试端戒指通信。
- 后端生成 `ringfitness_imu_lp_acc_50hz_*.csv`，作为算法阶段的主要输入。
- 独立活动同步、导入和分析可在无 Oura、Junction 与睡眠数据配置的条件下运行。

### 3.5 算法阶段

- 以 50 Hz 三轴加速度和 session 级参考步数为基础数据。
- 先检查数据质量，再汇总同一 session 的全部对应信号，比较估计总步数与唯一参考总数；多个文件共享 session，参考值只参与一次比较。
- 新记录的走路/跑步选择表示被试声明的 session 任务，活动纯度仍需核对；逐样本真值保持未标注。历史自由活动记录保持原场景和标签含义，冻结包保持原内容。
- 后续可从加速度研究活动识别；评价“其他活动、走路、跑步”三分类准确性时另行采集明确的活动时间标签。计步器总数仅提供整段参考，无法提供逐步时刻或活动边界。算法标签须记录来源及模型版本。
- 卡路里估计在步数与活动识别达到稳定质量后进入独立研究阶段。

## 4. 数据契约

每个 session 的 `manifest.json` 至少应准确表达以下语义；字段细节及兼容方案在功能规格中定义：

- `session_id` 与被试编号；
- App版本、包创建时间及数据契约版本；
- `capture_purpose = daily_activity`；
- 本段走路/跑步选择及被试声明来源；活动时间标签保持缺失，历史自由活动记录按原契约保存和读取；
- 戒指佩戴手与手指；
- 采集开始和结束时间及可信程度，开始/停止请求与确认、设备时间信息和样本实际覆盖范围；
- `ground_truth_source = external_pedometer`；
- `ground_truth_steps`：`valid` 与 `unreliable` 均保存 0 或正整数；`missing` 保存 null。`unreliable` 表示取得数字但可靠性受影响，`missing` 表示无法提供数字；两种异常均保存原因；
- `ground_truth_recorded_at_ms`：确认数字时的 UTC Unix 毫秒，未确认数字时为空；
- 下载完成时间，独立于采集结束及填写时间；
- 数据完整性、佩戴中断和计步器异常等状态，以及已知或未知的异常时间范围；
- `.rfbin` 文件信息与 SHA-256。

`ground_truth_steps` 表示整个 session 的总步数，通过 `session_id` 与该段所有原始文件关联。下载耗时不计入采集时长；边界不明或信号覆盖不足时保留异常及其证据，待研究者判定分析资格。

每日汇总按已记录的时区计算，只合计有效、互不重复且时间范围清楚的 session，并报告覆盖区间。未佩戴、漏采或未开始的时间保留为缺失。界面和报告区分“当天已采集时段的累计步数”与“覆盖完整目标时段的全天步数”；后者需有预先定义的目标时段和完整覆盖证据。跨午夜且只有一个参考总数的 session 保持整段参考，日汇总单列为跨日未分配，避免按时间比例或整段归日推定两天参考步数。

## 5. 核心原则

### 5.1 被试负担最小化

界面围绕单一采集任务组织。每个页面提供一个清晰的主要操作，减少培训、记忆和重复输入。

### 5.2 数据完整性优先

计步器步数确认后立即持久化。取得数字但可靠性受影响时保存 `unreliable`、数字和原因；无法取得数字时保存 `missing` 和原因。两类情况均保全原始数据。元数据安全保存后再执行 Flash 下载、压缩和上传。任何失败都应形成可理解、可恢复的状态。蓝牙断连与信号缺失分别记录，完整性依据戒指实际保存的数据判断。

### 5.3 一次采集，一个可追溯 session

采集信息、戒指原始数据、计步器步数、异常和上传回执共享同一个 `session_id`。一个 session 可包含多个文件，文件切分、重连和重试均保持该身份；所有派生数据可追溯到原始 session。

### 5.4 复用经过验证的底层能力

开发以 Android 0.5.3 为实验设计与设备实现的信任基线。原版的实验字段、操作顺序、戒指协议、Flash 格式和恢复机制默认保持；当前 App 与原版不一致时，须在对应规格的差异记录中列出原版行为、当前行为、改变依据、数据影响和验证证据。

差异分为三类：应恢复但尚未对齐的缺口、项目负责人已确认的新实验要求、证据不足而暂不调整的事项。只有明确的新需求或验证证据可以改变原版行为；界面精简不得删除仍有实验含义的字段或改变采集数据语义。原版中与本研究无关的旧平台、睡眠、外部设备和主观评分功能可按已确认范围退出，并保留差异说明。

发现尚未登记的字段、默认值、操作顺序、设备命令或恢复规则差异时，应在实现前写入差异记录，并在当轮汇报中主动提出。涉及实验含义、数据解释或被试操作的差异须经项目负责人确认；确认前沿用原版行为。技术增强也须说明原版行为、增强依据、数据影响和验证证据。

页面负责呈现状态与提交用户操作，采集协调、设备通信和持久保存各有明确职责。关键数据由指定模块统一维护，页面依据对应结果更新；演示与正式运行复用页面和业务规则，仅在设备、传输边界使用受控替身，测试数据保持隔离。结构调整围绕当前需求展开，并检查重复逻辑、状态归属和恢复行为。

### 5.5 隐私与密钥隔离

实验数据使用项目规定或 App 生成的匿名被试编号。显示名称仅保存在手机本地。上传链接、token、enrollment code、个人数据和本机路径只保存在受控环境中，不进入 Git 历史。

### 5.6 可验证与可复现

每项功能都要有明确的验收条件。数据转换保留原始文件、版本、校验值和处理记录，使研究结果能够复核。

### 5.7 案例驱动学习

每个阶段同时交付可运行成果和学习说明。说明应回答：相关文件负责什么、数据如何流动、本次修改解决什么问题、如何验证结果。

## 6. Roadmap：开发路线图

以下阶段表达能力建设方向与依赖关系。执行计划按可验收的用户任务组织交付，可以跨阶段接通页面、业务与数据，并行开展不冲突的准备工作。设备取证和故障恢复验证随相关功能推进；阶段4汇总完整链路、长时和独立操作证据，阶段5整理可正常使用的版本交付。实际采集与发布依据对应验收结果和已验证适用范围。

### 阶段 0：建立项目基线

- 批准并提交本总纲；
- 保留原始交接包，建立 Android 与后端工作副本；
- 配置 Android SDK、Java、Python 和本地密钥模板；
- 运行现有单元测试并构建 0.5.3 调试 APK；
- 将源码、构建工具、APK 和现有测试报告对应起来，形成桌面基线记录。
- 原版登录后的真机全流程由独立版本的验证承接；原版未执行的检查按重新规划记录，不记为通过。原始证据保留。

### 阶段 1：极简 Android 采集流程

- 建立独立名称、安装身份与数据目录，验证与原版共存；
- 首次本地填写研究编号或显示名称，记住匿名数据身份，移除旧平台登录及授权依赖；
- 首页提供走路/跑步选择，每段显式选择后通过同一开始入口采集；
- 保留被试编号、戒指连接，记住已确认佩戴位置；
- 以唯一首页承接设备、活动选择和记录；开始确认、进行中、结束确认复用同一采集页，参考读数与三种收尾复用同一收尾页；
- 空闲时允许更换当前身份、佩戴位置和戒指；已有 session 保持创建时快照，不随之后的设置变化；
- 隐藏当前实验不使用的功能；
- 在独立版本中完成戒指连接检查；完整采集和恢复检查随阶段 2、3 的数据闭环执行；
- 相关功能达到验收条件后，继续执行计划中依赖已满足的下一项；真实采集入口须具备停止、参考保存和原始数据保全出口。

### 阶段 2：计步器参考步数

- 在结束流程中加入本次总步数输入与简短异常记录，落实每次开始前清零、站定开始和停止后读数的提示；
- 将步数、来源、记录时间、有效性及边界证据持久化到本地 session；
- 支持重启恢复与确认前修改，确认后的纠错方式在功能规格审阅后确定；
- 将参考步数、session 活动选择与来源、质量状态及文件归属写入 manifest；
- 为输入校验、持久化和 manifest 生成增加测试。

### 阶段 3：后端数据闭环

- 校验走路/跑步 session 契约、参考字段和质量状态，历史自由活动包按原有契约识别；
- 在导入历史与处理账本中保留字段；
- 使用 `activity_session_id` 关联 CSV 与步数；
- 对同 session 的多文件汇总评估，逐样本保留未标注状态；任务选择与算法/人工时间标签分别表达；
- 实现可追溯的 session 级结果与日汇总规则；
- 验证重复上传的幂等性；
- 提供研究者可检查的导入结果。

### 阶段 4：端到端真机验证

- 配置测试清华云盘上传链接；
- 完成走路后跑步两段独立采集，以及误混/间歇、同日多次和零步检查，再按设备表现逐级延长；
- 实测电量、存储容量/增长、采集边界、信号连续性、长记录下载和恢复耗时；
- 覆盖重启、断连、上传失败和重试场景；
- 逐次核对 App、manifest、后端记录和 CSV；
- 由按操作说明准备的测试者独立完成采集、保存、上传及必要恢复，研究端正确读回；
- 形成可复现的验收记录。

### 阶段 5：可用版本交付

- 在开发验收中解决妨碍独立完成、数据完整与正常恢复的问题；
- 整理可安装 APK、已验证手机/戒指/固件和采集时长、被试操作说明及已知限制；
- 固定交付版本与数据协议，支持被试正常采集及研究端自动读取。

按用户于2026-09-21确认的交付目标，取消独立试运行阶段和预设采集天数。人数与周期由实际研究安排决定；软件按用户自主起止的 session 工作。设备能力、时间对应和数据质量继续依据实测记录及现有规则验收。

### 阶段 6：算法研究

- 建立数据质量检查和可视化；
- 实现步数估计基线并定义评估指标；
- 研究活动识别，并为分类准确性评价另行设计带明确时间标签的补充采集；
- 在独立训练集和测试集上报告结果；
- 根据验证结果评估卡路里研究的可行性。

## 7. 功能开发工作流

### 7.1 交付单位与完成条件

先明确完整用户流程、数据流和最小视觉规则，再按可独立验收的用户任务实现。每项交付写清入口、主要操作、退出或恢复路径，以及可核对的数据结果。底层模块可先测试、独立提交，并注明服务的用户任务及接入步骤。

技术基础完成、演示可操作和真实链路已验证分别记录。功能交付需同时满足相关页面、行为、数据与验证条件；演示只证明其标明的软件范围。及时展示可运行的中间成果，正式验收仍覆盖该项全部完成条件。已验收界面按后续任务需要调整，视觉细节集中处理。

### 7.2 实现与反馈

每个交付遵循以下循环，内部技术步骤可以小步提交：

1. **Specification：** 写清用户、场景、输入输出、状态、异常路径和验收条件。
2. **Implementation：** 在既有架构内完成最小范围修改，为重要行为补充或复用自动化测试。
3. **Validation：** 按改动影响执行检查，功能交付时运行对应页面并核对数据产物；设备相关结论由真机证据支持。
4. **Replanning：** 根据验证结果更新当前项、下一项及阻塞，修复影响本项验收的问题后继续推进。涉及使命、范围或原则的变化先确认并修订总纲。

### 7.3 自主推进与决策边界

执行计划集中维护当前任务、完成条件、证据入口、下一项及阻塞原因。每次开始或恢复工作先核对实际代码与验证状态；当前项完成后，开发代理依据第8节优先级和技术依赖，继续最高优先级、条件具备且已获授权的下一项。以一个主要交付为当前重点，独立准备和只读审查可并行，修改范围先明确再集成验证。

常规实现、修复、测试和既定视觉规则内的调整自主完成。研究规则、数据含义、需求范围和待定的重要产品选择由项目负责人决定，已确认事项沿用。遇到阻塞先完成范围内排查，再说明缺少的条件、建议与受影响任务，同时推进不依赖该条件的工作。需要设备操作时给出具体步骤。

### 7.4 验证与代码审查

检查随风险和影响范围选择：文档检查链接、引用及一致性；视觉调整检查相关页面与可访问性；导航、状态、保存及设备变更补充对应行为、恢复和集成验证。功能验收仍须满足既定条件，分别报告自动测试、构建、模拟与真机的结果、跳过项和限制，既有失败保留跟踪。

普通文案由主代理自查；涉及蓝牙、采集状态、保存恢复、上传或导入的重要修改，在可用时安排独立只读审查，指出位置、触发条件、影响与依据。主代理核实后修复，重要缺陷增加行为回归测试。复用可重复执行的构建和快速检查命令，保持验证入口可供团队使用；团队仓库接入时配置提交或合并触发的自动检查。新增回归优先修复，纯润色建议可进入后续计划。

### 7.5 文档与版本协作

总纲维护长期原则；requirements维护行为和数据契约；plan维护顺序、依赖与进度；validation维护证据和限制；既有change-review保留必要设计依据。只更新受影响部分，概览链接到对应事实，详细历史由Git追溯。原始日志、截图、本机配置和个人偏好按本地规则保存。

每轮说明实际可操作范围、验证证据与限制、接下来的任务和需要负责人决定或操作的事项。相关修改审阅、验证并核对差异与敏感信息后，自行提交本地Git，保留其他工作范围；远程推送、发布及授权范围外的重要行动按会话授权另行确认。工作流按需求或证据修订，日常进度更新执行计划。

## 8. 决策顺序

出现方案冲突时，按以下顺序判断：

1. 被试安全、隐私与数据完整性；
2. 研究数据的有效性和可追溯性；
3. 被试的操作负担与流程清晰度；
4. 系统稳定性与可恢复性；
5. 开发速度与实现便利。

## 9. 外部依赖

进入端到端验收前，项目负责人需要提供：

- 一枚可用的测试戒指、充电盒与固件版本；
- 一台 Android 11 及以上真机；
- 测试与正式清华云盘上传链接；
- 被试编号规则与测试编号；
- 后端部署位置和测试环境；
- 正式实验的数据管理与伦理要求。

## 10. 总纲修订规则

- 总纲采用语义化版本号。
- 文字澄清和不改变行为的调整提升补丁版本。
- 范围、流程或技术边界变化提升次版本。
- 使命、数据契约或核心原则发生根本变化提升主版本。
- 每次修订应记录原因、影响范围和验证要求，并通过 Git 提交保留历史。

### 修订记录

| 版本 | 依据与变化 | 影响与验证 |
| --- | --- | --- |
| 3.3.1 | 2026-09-21 项目负责人重申：所有与原版设置不同的内容均须主动提出，原版作为实验设计信任参考 | 新差异先登记并在当轮汇报；实验含义、数据解释或被试操作变化确认后实施，确认前沿用原版；技术增强保留依据和证据 |
| 3.3.0 | 2026-09-21 用户要求以原版0.5.3作为实验设计信任基线，并收敛被试端页面层级 | 所有差异按“应恢复／已确认改变／证据不足”登记；身份类型显式区分，保留原版六种佩戴位置及字段；唯一首页、同一采集页和同一收尾页须通过数据与恢复验收 |
| 3.2.0 | 2026-09-21 用户确认以可正常使用为开发交付目标，取消独立试运行阶段和预设采集天数 | 真实设备、关键恢复、长时及独立操作纳入开发验收；交付公布已验证设备、时长和限制。采集与数据契约保持原义，历史证据保留原适用范围 |
| 3.1.0 | 2026-09-20 用户确认停止后的保存上传、持久暂缓及二次确认放弃 | 本地完成后释放下一段，上传遵从持久选择；放弃仅清理本段手机数据并排除重取，戒指物理删除以已核实协议为限。软件及设备验收分别记录于E26 |
| 3.0.0 | 2026-09-19 用户确认新采集按走路、跑步分段，每段重新选择、清零并填写独立参考总数；活动选择仅表示任务声明 | 升级活动数据契约，联动首页、session 冻结、恢复、上传及后端；验收走路完整保存后再跑步，逐样本仍未标注；历史自由活动账本、标签和冻结包保持原义。随0.8.0-session-finish集成交付，证据见E26 |
| 2.1.0 | 2026-09-19 用户授权修订工作流：按用户任务交付、区分技术与运行证据、依赖驱动的自主推进、按风险验证及文档分工 | 变更开发流程，使命、实验操作、数据契约与待确认交互保持原义；与当前执行计划对齐，文档验证单列，真实采集和试用门槛继续适用 |
| 2.0.0 | 2026-09-18 用户明确自由活动、每次清零、自主起止、整段一个参考数，并在后续消息确认总纲、授权小步实现与验证 | 取消固定活动入口及5+5判据；联动采集/数据契约并增加长时验证；补充分风险审查与证据分层流程。功能规格Q01–Q03及设备实测状态仍分别保留 |
| 1.1.0 | 2026-09-18 用户明确要求独立的计步实验 App、本地记住编号及独立安装身份；移除旧平台账号依赖 | 调整阶段 0/1 的验证顺序，原版桌面基线继续保留；独立版本须验证离线登记、共存、数据归属及完整采集恢复。实验操作和重要交互的未决细节仍在规格中待确认 |

本地提交可保存待审阅草稿，草稿中的待确认事项继续保留；提交、推进与决策边界统一按第7节执行。
